# Snippets

Portable, reusable code — write once, use forever. Organized by language first.

Add a language folder (`bash/`, `python/`, `javascript/`, etc.) the first time it's actually needed. Add function-based subfolders inside a language folder only once it's crowded enough to justify the split.
